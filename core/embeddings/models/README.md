# Model Files

Models are downloaded on first run, not bundled in repo.

To download:
```bash
cd core/embeddings
python3 -c 'from sentence_transformers import SentenceTransformer; SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2")'
```
